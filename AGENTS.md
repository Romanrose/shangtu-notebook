# 时空探索手札：Agent 工作约定

本仓库实现的是一本会回应的手写书。纸页体验是产品，Pi、CNKGraph、视觉识别和服务端只在背后服务它。

本文件将项目约束与目标驱动工程方式合并。它偏向谨慎而非速度；对明显的一行改动可酌情简化流程，但不得突破任何产品边界。

## 先想清楚，再动手

处理非简单任务前，先阅读本文件、`docs/mvp-contract.md` 和最小相关代码面；涉及 Pi 或服务端时再读 `docs/pi-tool-contract.md`，涉及回应结果时再读 Skill 中的验收矩阵。

在编辑前，用简短文字声明：

- `Goal`：可观察、可验证的结果。
- `Constraints`：本次适用的产品和技术边界。
- `Smallest change`：实现目标所需的最小改动。
- `Not changing`：刻意保持不动的邻近范围。
- `Acceptance checks`：完成前必须运行或人工确认的检查。

不应静默猜测。若需求存在会改变纸页交互、事实与联想的边界、Pi 工具权限、数据来源或交付范围的实质歧义，应列明选项和影响后等待用户决定。仓库中已有的信息无需再向用户追问。

## 保持简单，做手术式改动

- 只实现用户请求及其验收所必需的内容；不为单次用途添加抽象、配置层、依赖或“未来扩展”。
- 每一处修改都应能追溯到用户目标或本文件明确的不变量。
- 不顺手重构、不重排、不改无关注释或格式；发现既有问题应报告，不应擅自删除。
- 仅清理由本次修改造成的死代码、未使用导入或失效测试。
- 先做可验证的纵切面，再考虑视觉润色、泛化能力、页面历史或代理能力。

## 产品不变量

- 这是手写书，不是聊天界面或图谱可视化工具。纸页是唯一主界面；静读/寻迹可切换，其他操作嵌入纸页。
- 原始笔迹必须保存，且不得被 AI 覆盖。OCR/转写只是可编辑的机器版本。
- 寻迹模式停笔后 1 秒内必须仅靠本地状态进入“停笔反馈”：墨迹呼吸、关系纹路起笔与“识字中”。网络、模型或图谱延迟不得阻塞它。
- 静读模式只记录笔迹，不调用 Pi 或显示寻迹反馈。
- 优先服务华为平板 PWA。触控笔和触摸都必须可书写，并避免浏览器滚动抢占书写手势。

## 证据与模型边界

- 证据旁批只能陈述有可追溯来源支持的事实、关系、日期、人物或馆藏记录，并能展开来源与有界寻迹路径。
- 模型文化补充不构成事实，必须显式以“联想：”标注，并在纸面上与证据旁批区分。
- 实体不唯一或转写低置信度时，必须给出可编辑转写或候选；不得静默选择实体。
- 没有可靠路径时，必须明确呈现“证据缺口”；不得编造来源、年代、人物关系或记录。

## Pi、数据与安全边界

- Pi 是受限的运行时内核，不是开放式文件或网络代理。允许的能力仅限：转写、实体澄清、CNKGraph 检索、有界子图裁剪、证据核验与旁批生成。
- 当前 Pi 运行时的精确工具白名单、初始化禁用项和服务端边界以 `docs/pi-tool-contract.md` 为准；变更工具面必须同时更新合同与合同检查。
- 浏览器不得持有模型、CNKGraph 或其他服务端凭据。前端只提交必要的笔迹段图像与转写，受控服务端负责模型和图谱调用。
- 旧项目 `/home/romanrose/Project/shangtu` 仅可只读参考。只迁移已验证的查询和来源追溯能力，绝不迁移其旧图谱优先前端。

## 目标驱动执行与验证

把任务改写成可验证目标，而非含糊地“做出来”。例如：

- 修复交互问题：先构造复现步骤，再确认修复后步骤通过。
- 改变寻迹结果：验证证据、歧义、证据缺口三分支及标签边界。
- 改变笔迹或页面状态：验证停笔一秒内本地反馈、静读不寻迹、翻页后笔迹仍保留。

多步骤工作应按“步骤 → 验证方式”推进，并在每个可验证增量后运行最窄检查。若依赖可用，受影响的改动还应运行：

```bash
npm run check
npm run build
npm run check:agent
```

交互改动不能只以桌面渲染为完成标准。交付时记录一项具体的华为平板检查：横竖屏各一次，触控笔与手指各一次，确认书写、停笔反馈及滚动手势符合预期。

## 交付说明

完成时简要说明：改动文件及原因、实际运行的命令与结果、覆盖的验收用例、假设/缺口/明确延期项。不得把未执行的检查写成已通过。
